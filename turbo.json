{
    "$schema": "https://turbo.build/schema.json",
    "globalDependencies": [
        "**/.env.*local"
    ],
    "globalEnv": [
        "SUPABASE_SERVICE_ROLE_KEY",
        "GATEWAY_INGEST_KEY",
        "CLERK_SECRET_KEY",
        "CRON_SECRET",
        "OPENAI_API_KEY",
        "GEMINI_API_KEY",
        "BIGDATACORP_TOKEN"
    ],
    "tasks": {
        "build": {
            "dependsOn": [
                "^build"
            ],
            "outputs": [
                ".next/**",
                "!.next/cache/**"
            ]
        },
        "lint": {},
        "dev": {
            "cache": false,
            "persistent": true
        }
    }
}
